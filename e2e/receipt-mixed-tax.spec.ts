import { test } from "@playwright/test";

// Phase 5: レシート明細(lines)と税率混在対応の検証スイート。
//
// 設計:
//   1レシート = N明細 (tks_receipt_lines)。
//   税率/勘定科目混在のレシートは複数行で保存され、
//   親 receipts の amount / taxAmount / taxRateCategory は lines から自動集計される。
//
// 認証配下のフローのため、Firebase Auth + Supabase の認証モック基盤が必要。
// 基盤整備後に test.skip を外して有効化する。
test.describe("レシート明細・税率混在対応（Phase 5 Step 2）", () => {
	test.skip("1レシートに 8% / 10% を1行ずつ作成 → 保存 → 再オープン → 合計一致", async () => {
		// REQ-RC-01〜05
		// 1. レシート起票画面で「行を追加」して2行に
		// 2. 1行目: 税率10%、税込1100、勘定=消耗品費、品目=ボールペン
		// 3. 2行目: 税率8%、税込540、勘定=福利厚生費、品目=お茶
		// 4. プレビュー欄に 合計=1640 / 税額=140 / 税率区分=混在 が表示される
		// 5. 保存 → 詳細画面の「明細」テーブルに2行表示
		// 6. 一覧画面で 金額=1640、税率区分=混在 と表示
		// 7. 編集画面を開くと2行のドラフトが復元される
	});

	test.skip("単一行(税率10%)レシートも明細1件として保存・再表示される", async () => {
		// REQ-RC-01
		// 1. 行を追加せず1行のみで保存
		// 2. tks_receipt_lines に lineNo=1 のレコードが1件
		// 3. 親 receipts の taxRateCategory='10'
	});

	test.skip("0件で保存しようとするとバリデーションエラー", async () => {
		// REQ-RC-01 (invariant): 1レシート = 1行以上
		// 1. 全行の税込金額を空欄にして保存ボタン押下
		// 2. 「明細を1行以上、有効な値で入力してください」のエラー表示
		// 3. 保存されない
	});

	test.skip("明細編集後、親 receipts の集計値が自動更新される (REQ-RC-04)", async () => {
		// 1. 既存の単一行レシート（10%、1100円）を編集画面で開く
		// 2. 税込金額を 2200 に変更して保存
		// 3. 親 receipts.amount=2200、tax_amount=200 に更新される
		// 4. tax_rate_category='10' のまま
	});

	test.skip("AI抽出結果が lines 配列を含む場合、ドラフトに展開される (REQ-RC-02)", async () => {
		// 1. /api/receipts/extract に画像をPOST
		// 2. レスポンスの extraction.lines が2件以上
		// 3. 編集画面に2行のドラフトが事前入力される
	});

	test.skip("AI抽出結果が旧形式(lines無し)でも単一行として展開される (後方互換)", async () => {
		// 1. extraction.lines が空配列のレスポンスを受信
		// 2. フォームに amount/taxRateCategory から合成された1行が表示される
	});

	test.skip("混在レシートの一覧表示で「品目」列が見える", async () => {
		// US-3.1 / 一覧改善
		// 1. レシート一覧画面で品目カラムが default visible
		// 2. 各レシートの itemName が1行表示される（1件クリックしなくても把握できる）
	});

	test.skip("CSVエクスポートに 品目 / インボイス区分 / 申請区分 / 申請登録日 列が追加されている", async () => {
		// Phase 5 でCSV列を拡張
		// 1. 一覧画面の「CSVエクスポート」を押下
		// 2. ダウンロードされたCSVのヘッダに「品目」「インボイス区分」「申請区分」「申請登録日」が含まれる
		// 3. 各行に該当値が出力される
	});
});
